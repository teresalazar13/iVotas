Example running the RMI:
java -jar dataserver.jar IPAddress UDPPort RMIPort

Example running the admin:
java -jar console.jar IPAddress RMIPort RMIBackupPort

Example running the tcp server:
java -jar server.jar IPAddress serverPort RMIPort RMIBackupPort VotingTableID

